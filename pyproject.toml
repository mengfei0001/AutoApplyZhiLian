[project]
name = "py-zhaopin"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pymysql>=1.2.0",
    "pytest-playwright>=0.8.0",
    "httpx>=0.27.0",
    "sqlalchemy>=2.0.52",
]

[tool.uv]
# 这里直接指定 index-url，比 registries 更通用且不易出错
index-url = "https://mirrors.aliyun.com/pypi/simple/"

